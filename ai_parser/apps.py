from django.apps import AppConfig


class AiParserConfig(AppConfig):
    """Configuration for the AI Parser application.
    This application is responsible for parsing and processing AI-related data."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ai_parser'
